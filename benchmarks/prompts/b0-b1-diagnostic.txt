Tesy diagnostic baseline prompt. Sparse mixture of experts models route each token through a subset of available experts while shared attention and routing components remain active. This diagnostic text is intentionally synthetic and repetitive so the workload can be committed with the repository and reused without external data. The benchmark compares stock automatic placement with the stock CPU-MoE placement control under the same exact model, prompt, context and deterministic generation settings.

Measure the runtime rather than assuming which placement is faster. Report prompt processing, first streamed output latency, decode throughput, memory pressure, GPU memory, temperature and placement logs. Do not infer physical storage or PCIe traffic from encoded tensor sizes. Negative results are valid. Repeat the same comparison without changing quantization, model bytes, routing, top-k or generation semantics.
