# Tesy scale lab state

- Start: 2026-09-26 00:44 Europe/Lisbon (2026-09-25 23:44 UTC).
- Objective: exact routed inference of gpt-oss-120b above available RAM; primary target >=2 sustained decode tokens/s, then 4 and exploratory 8. Task benefit is a separate gate.
- Main state: **MODEL_ARTIFACT_REQUIRED**. M0_BACKEND_SELECTED and M1_OVER_BUDGET_END_TO_END were demonstrated on the 20B surrogate only. M2 is partial; M3, M4, M5 and M6 are not demonstrated. Diagnostic configuration interventions: 2/6. About 40 minutes of the four-hour ceiling were used; the artifact gate ended this session early.
- Lab location: `/home/pmglo/Projects/Tesy/tesy-scale-lab`. Preferred sibling path was absent and outside writable roots. This is a separate nested Git repository on branch `scale-lab`; the pre-existing Tesy branch/worktree is untouched.
- Surrogate: local gpt-oss-20b MXFP4 GGUF, 12,109,564,352 bytes, SHA-256 `52f57ab7d3df3ba9173827c1c6832e73375553a846f3e32b49f1ae2daad688d4`, revision `a7443ebb00ba299cbbbf7e9b69487670447ae8c0`. Candidate stream and stock generated real responses. Two candidate CPU requests under a 9 GiB host cgroup ended at natural EOS after 195 and 193 decode tokens, with peak cgroup 7.35 GB, zero swap and zero OOM. A candidate GPU8 server answered a localhost chat request.
- Target: local `openai/gpt-oss-120b` original MXFP4 safetensors, revision indicated by local Hugging Face tree `b5c939de8f754692c1647ca79fbf85e8c1e70f8a`; seven shards total 65,248,815,744 tensor bytes. The compatible GGUF target artifact is absent, so the 120B was not executed. Full shard SHA-256 verification has not run. Public GGUF revision, size and SHA are locked in `models.lock.json`.
- Backends: stock `ggml-org/llama.cpp` at `4b1a27fa0eb875bbca4f6cfe936e3d65adc685c0`; one streaming candidate `freedomljc/llama.cpp` at `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`. The latter is a July 2026 snapshot and not established compatible with current stock.
- Last correct lab checkpoint before final results: `f4fa309`. Backend source is unmodified. Best validated streaming surrogate configuration: candidate SHA above, 20B GGUF, GPU8, 16 expert slots per layer, O_DIRECT, no preload, no repack, op offload disabled, context 4096, batch 256, microbatch 32, greedy. Fixed-prefix GPU8 logits were bitwise equal to same-SHA plain on 201,088 values; long free generation has narrower functional coverage. Practical stock 20B GPU14 was faster on the two-request workload.
- Host: physical Fedora laptop verified outside the sandbox: Ryzen AI 9 HX 370, RTX 4060 Laptop 8188 MiB, 30 GiB reported RAM, internal NVMe `/dev/nvme0n1p3` btrfs `/home`. `MemAvailable` at inventory 24.08 GiB; zram swap 15 GiB, unused. Driver 615.71.09, CUDA toolkit 13.3.73. GPU 45 C idle, up to 61 C in a short stock run; sustained thermal regime and power limit unknown. Cgroup v2 `memory.max=9 GiB` and `memory.swap.max=0` were enforced for main surrogate runs.
- Current bottleneck: target artifact format blocks M4 on the chosen backend. On the 20B surrogate, decode reads and limited expert capacity indicate real misses, but tier hit counts, exclusive physical NVMe traffic, H2D bytes and a sustained bottleneck attribution are unknown. Stock 20B fits in combined host/GPU memory and is faster in the exploratory practical A/B.
- Next gate: after the user installs and verifies the pinned 120B MXFP4 GGUF, inspect its actual tensor metadata and loader memory peaks, then run bounded one-sequence M4 load/prefill/decode and a fixed-prefix numerical comparison. No 120B execution command has been tested.

See `research/M0-M05-20260926.md` for the initial budget and `research/M0-M1-RESULT-20260926.md` for measured evidence, failures, alternatives and limitations.
