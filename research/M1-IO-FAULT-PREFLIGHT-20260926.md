# M1 synthetic read failure: prospective diagnostic

Objective: test one mandatory fail-closed property of the existing expert-streaming mechanism without changing backend source or model files. Base lab commit `7611d23`, candidate SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`, verified local 20B GGUF SHA-256 `52f57ab7d3df3ba9173827c1c6832e73375553a846f3e32b49f1ae2daad688d4`. Evidence class will be synthetic fault injection, not a throughput or 120B correctness result. This does not consume a throughput optimization intervention.

Use a small `LD_PRELOAD` library only in the spawned 20B candidate process to make the first `pread` on an O_DIRECT descriptor at an offset beyond the 4096-byte open probe return `EIO`. It does not write to, truncate, replace or copy the GGUF. The diagnostic prints a unique injection sentinel. The selected 20B GPU8 eight-token smoke keeps original routing, cache16, four I/O threads, no preload/repack, context4096, batch256/microbatch32, greedy seed42. Enforce a 9 GiB cgroup, zero swap, RSS8.5 GiB, GPU7000 MiB, MemAvailable6 GiB and 180-second timeout; disable core dumps. Run ID `fault20b-direct-read01`.

Predeclared pass for fail-closed behavior: the sentinel appears once, the process exits nonzero promptly before any committed output tokens, watchdog reports no timeout, swap, OOM or cgroup max event, and no experiment process remains. If the sentinel never appears, the injection is unverified and the test is inconclusive, regardless of model output. If the process hangs, emits tokens as success, or hits resources, the fail-closed test fails. This tests one read-error path only; it does not validate partial reads, arbitrary eviction in flight, GPU transfer errors, or every queue/cancellation state. Raw stdout/stderr/samples remain local; compact manifest/report will be committed. Tests: NOT_RUN at freeze.
