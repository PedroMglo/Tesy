# M4 target preflight, new campaign

Objective: test exact routed `gpt-oss-120b` GGUF MXFP4 above physical RAM with the previously selected streaming candidate. Primary performance target stays >=2 sustained decode tok/s; this smoke is a bring-up diagnostic, not that test. Start: 2026-09-26 10:52 Europe/Lisbon. Previous campaign ended at `MODEL_ARTIFACT_REQUIRED`; its failures and measurements remain unchanged. This is a new run identity with 0 new optimization interventions. Base Tesy commit/tree `7041b39ec42948e6d394e151974940750ba9b070`; separate lab branch `scale-lab`, base checkpoint `3dbf9f1`. Stock source SHA `4b1a27fa0eb875bbca4f6cfe936e3d65adc685c0`, streaming candidate SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`; both source trees clean. Existing GCC 15/CUDA 13.3/SM89 builds are used unchanged. No remote publication.

## Measured preflight

- Physical Fedora host exposes Ryzen AI 9 HX 370, 24 threads, 30 GiB displayed RAM, RTX 4060 Laptop with 8188 MiB VRAM, driver 615.71.09. `/home` is btrfs on internal `/dev/nvme0n1p3`; target and lab are on `/home`, with 393 GiB free at inspection. This source path is not an external SSD. A short `systemd-run --user --scope` check with `MemoryMax` and `MemorySwapMax=0` succeeded outside the tool sandbox. No thermal ceiling or sustained clock is inferred.
- Target GGUF at `/home/pmglo/models/gpt-oss-120b-gguf/gpt-oss-120b-MXFP4.gguf`: 63,387,346,208 bytes, full local SHA-256 `582bd40f6886200101f4c4ed9f25f3fe80cc14c86e9e2b37746cd8904a0c622d`, equal to locked published LFS SHA for `ggml-org/gpt-oss-120b-GGUF` revision `238abdd290bb874b90a5da1b4549881b7d05c091`. Hash proves file content identity relative to that lock, not runtime correctness. Source GGUF has 36 layers, 128 experts/layer, top-4, 8 KV heads of dimension 64, 687 tensors.
- From GGUF tensor sizes: streamable MXFP4 expert weights 60,914,073,600 bytes, 13,219,200 bytes per layer/expert across gate/up/down, including format scales and padding as encoded. Separate F32 expert biases 159,252,480 bytes; other tensors 2,300,997,888 bytes. Source audit: only `*_exps.weight` tensors are redirected to per-layer slots; biases keep original expert IDs. Automatic slots would be 16, but the run specifies 16 explicitly. Multi-wave prefill requires at least 12 for top-4. Existing candidate has no proved dynamic host cache tier.

## M0.5 prospective budget for first smoke

Host cap: 18 GiB cgroup, `memory.swap.max=0`, plus watchdog requiring >=6 GiB system `MemAvailable`. Four I/O workers. Initial `-ngl 8`, so at most 28 CPU layers of 16 slots account for 5,922,201,600 bytes encoded CPU expert cache. Non-streamed tensors total 2,460,250,368 bytes across host and GPU, exact split to be measured. Four aligned staging buffers use roughly 17.7 MB for the largest 4,406,400-byte per-tensor slice plus alignment slack. Host also needs loader state, GGUF metadata, page cache from non-experts, CPU KV share, backend work buffers, and any CPU shadow copies of GPU buffers. These are measured at load/prefill/decode; the nominal sum is not a peak claim. The guard leaves about 6 GiB outside the scope if availability permits.

GPU: 8 layers x 16 slots x 13,219,200 = 1,692,057,600 bytes encoded expert cache. Add GPU resident non-experts, KV, compute buffers, backend allocations and free margin; guard at 7000 MiB total use. For one sequence/context 4096, full BF16 KV upper calculation is 36 x 4096 x 8 x 64 x 2 (K,V) x 2 bytes = 301,989,888 bytes = 288 MiB, allocated across host/GPU according to backend placement. Actual KV and buffers must be read from load logs. Host and GPU resident copies are accounted separately; the GGUF on disk and page cache are not silently treated as free RAM.

## Frozen first gate

Run ID `target120b-gpu8-smoke01`. Candidate with streaming on, `--moe-stream-cache 16s`, four I/O threads, O_DIRECT requested and verified via descriptor/log; no preload, no repack, no predictor or speculation. One sequence, one request, context 4096, `-b 256 -ub 32`, eight CPU threads, greedy temperature 0, seed 42, built-in GPT-OSS template via CLI single-turn mode, prompt `Reply with the sum of 7 and 5, then stop.`, up to 8 generated tokens. Cold process, filesystem page cache condition recorded as unknown/warm from hash scan; O_DIRECT expert reads bypass that cache if active. Stop at 600 seconds, 18 GiB cgroup max event/OOM/swap, 7000 MiB total GPU use, or <6 GiB host availability. Output stays under `results/`, with stdout/stderr and JSON samples untracked. A full two-request run and exactness probe require this smoke to pass. No threshold or model change may retroactively pass this gate.

Alternatives: stock full-resident target under physical RAM is not assumed viable; short fixed-prefix stock reference may be attempted if bounded and within resources. Native safetensors are a different artefact and are not silently substituted. Source parity evidence from 20B is a prerequisite clue, not 120B validation. First smoke is NOT_RUN at this preflight checkpoint.
