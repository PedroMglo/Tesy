# Campaign 2 protocol, frozen before new model execution

Campaign ID `c2-20260926-1349utc`; branch `campaign/c2-correctness-usability-20260926-1349utc`; base lab commit `c67529e290844bb3d9033615072d5878014a46bc`, clean at branch creation. Work began 2026-09-26 13:49:33 UTC. The independent `scale-lab` remote was at the same SHA; stock source SHA `4b1a27fa0eb875bbca4f6cfe936e3d65adc685c0`, candidate source SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`, both local trees clean. No remote writes are authorized by this campaign.

Objective: independently test correction breadth, sustained target performance and practical utility. Historical campaign 1 records remain immutable. Maximum eight hours elapsed **during active work**, including builds, model execution and waits, or an earlier session limit; pause intervals count only if experiments are stopped. Maximum six new optimization configurations. Reserve at least the final two hours for a predeclared new task evaluation and closure; stop optional performance screening first. A new test must fit the remaining time. Decision order: C2-A gates and historical reanalysis; freeze new tasks; C2-B target correction; C2-C sustained32; C2-D measured latency and at most justified optimizations; C2-E task utility; C2-F launcher and closure. Numeric coverage gaps block promotion of a new runtime default.

Identified physical host at start: AMD Ryzen AI 9 HX 370, 30 GiB reported memory, `MemAvailable` 27,261,704 kB; RTX 4060 Laptop 8188 MiB, driver 615.71.09, 13 MiB initial GPU use and 39 C; internal `/dev/nvme0n1p3` btrfs at `/home`. The target GGUF exists at 63,387,346,208 bytes on that filesystem; full SHA-256 from campaign 1 is `582bd40f6886200101f4c4ed9f25f3fe80cc14c86e9e2b37746cd8904a0c622d` and must be checked again before new heavy model tests. Swap globally had 1.1 GiB used by other activity; runtime/cgroup swap must remain zero. A sandbox `nvidia-smi` call failed, so GPU-bearing claims use only host-accessible measurements. The initial NVIDIA power query returned implausible idle 590 W; power telemetry is UNKNOWN until independently checked. The current thermal limit is to be set from sensor capabilities and observed idle/headroom before model execution, not from historical peak.

Production envelope: a user cgroup with `memory.max=18 GiB`, `memory.swap.max=0`; watchdog RSS<=17 GiB, total GPU<=7000 MiB, system MemAvailable>=6 GiB. One model process at a time, localhost-only servers, timeout per frozen protocol. No swap, OOM, backend error, missing telemetry or unexplained numeric discrepancy can be turned into a PASS. Selected model, quantization, routing, top-k, tokenizer, attention, KV and sampler remain unchanged.

C2-A model-free gate first. New-run evidence schema version `c2-run-v1` rejects unexpected/duplicate keys, NaN/Infinity, wrong types, mismatched identities, request IDs/order/timing/counts, resource violations, missing required sensors and incomplete samples. Frozen new-run telemetry: target 1 s cadence, maximum 3 s gap over measured activity, first and last sample within 2 s of the process boundaries. Historical `v1` manifests use a named legacy reader with actual raw files and explicitly limited claims, never synthetic retroactive fields. Historical source hashes identify the local files **now**, not their exact past contents. A false PASS in the old analyzer is preserved as a defect, not used as C2 approval. The new output root uses distinct `c2-` IDs and no replacement of existing files. Raw logs, samples and vectors stay local; compact results, tests and ledgers are committed locally.

Historical state at freeze: 24-slot server M5 reported 2.733 tok/s over 839.312 s decode in 1342.188 s whole-process; 32-slot only three short A/B pairs averaging 3.269 tok/s, sustained NOT_RUN; ten-task M6 tied 8/10; target bitwise numeric evidence consists of one 201,088-element final-logit vector after four tokens. These are prior observations, not C2 measurements. No expected C2 PASS is declared.

Alternatives: backend migration, new Transformer runtime, speculative decoding and new models are outside this campaign without a concrete blocker. Tests at freeze: branch/source/hardware inventory only; C2-A through C2-F NOT_RUN. Next gate: reproduce the three identified model-free false-approval defects and implement a strict validator before any new model timing.
