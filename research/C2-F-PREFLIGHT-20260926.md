# C2-F monitored launcher and context test preflight

State at 2026-09-26 15:19 UTC: DRAFT LAUNCHER, NOT_RUN. The `tools/c2_launch_target.py` command derives the pinned 32-slot server command from the same frozen C2-C protocol, checks the backend tree/binary/library/workload identity, verifies the complete 63,387,346,208-byte GGUF SHA-256 via read-only O_DIRECT requested without an application fallback **before** starting the timed/monitored server, then runs it under `memory.max=18 GiB`, `memory.swap.max=0` with the existing watchdog: RSS17 GiB, GPU total7000 MiB, host MemAvailable6 GiB, CPU95/GPU80/NVMe70 C. It binds only 127.0.0.1:18367, context4096, parallel1, no prompt cache, medium template, O_DIRECT expert reads and 32 slots. Clients must explicitly request temperature0, seed42 and output cap for comparable results. This is a diagnostic profile while C2-B production numerical parity is blocked; the launcher must not be called robust daily use merely because it starts.

The launcher is conditional on the C2-C sustained32 resource/timing gate. If that gate fails, freeze a separate compatible fallback rather than silently changing this command. Initial unit verification of its current/frozen identities passed model-free; the full artifact hash and an actual server launch remain NOT_RUN. The O_DIRECT verifier passed on a small disposable file with a nonaligned EOF. `O_DIRECT` is requested; filesystem/kernel physical I/O semantics are not inferred from the flag alone. Hashing occurs outside the timed model process. Run results have unique IDs and no-replace raw manifests.

Operation gate: start the server, check `/health`, send one normal localhost request, then a sequence of requests, cancel one client stream, verify the server can handle another request, stop only this process using `tools/c2_stop_server.py`, and confirm port/GPU release. The stop helper refuses a PID whose executable, model argument, localhost bind, process group and cgroup differ from its sampled identity; unrelated-PID rejection passed model-free. No process is left running at campaign end.

Context gate: `tools/c2_multiturn_probe.py` uses three frozen synthetic turns, JSON answers about a session key, max256, medium, greedy/seed42, no prompt cache. `QUESTIONS` canonical SHA-256 is `88b010f99e2a518b362f26b1c84fbda75fbe1d4b5b1d27315e98546130f9c7b2`. The client counts tokens after `/apply-template` and `/tokenize` before each request, requires actual API prompt counts to increase, checks zero cached prompt tokens, refuses `prompt+256>4096` and any `finish_reason` other than stop. The first turn acknowledges the key; turns two and three must return the exact key. This is a growing-context test, not a claim that all 4096 positions have been exercised. Its actual maximum occupancy and any backend context-shift log must be reported after execution.
