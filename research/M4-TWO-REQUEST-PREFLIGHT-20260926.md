# M4 two-request target gate

Frozen before run ID `target120b-gpu8-two-turns01`. Objective: verify end-to-end exact-routed generation on the 63,387,346,208-byte locked `gpt-oss-120b-MXFP4.gguf` under physical 30 GiB RAM, while preserving the 2 tok/s sustained target for later M5. Base lab commit `0507e10b0a03cbb9c5fe248efe719b946bd40309`; original candidate source SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`, untouched by the diagnostic reference patch. Full GGUF SHA-256 and four-token numerical parity are recorded in earlier gates. The latter is limited coverage; this run must not be described as bitwise validation of all generated tokens.

Command: `tools/run_target_two_requests.sh target120b-gpu8-two-turns01` from the lab root. It fixes the embedded GPT-OSS chat template via CLI conversation mode, one process, one sequence, two distinct public prompts in `workloads/m1_two_turns.txt` (SHA-256 `54329122bb29db556fdd12a517a298797c813666768d4866de7687e7648d2c31`), context 4096, batch 256, microbatch 32, 8 CPU threads, 8 GPU layers, 16 expert slots per layer, four O_DIRECT expert I/O workers, no preload/repack/speculation, greedy temperature 0, seed 42, up to 256 tokens per answer or natural EOS. Cold process; cache may be warmed by prior hash/probes, but direct expert reads bypass page cache if confirmed. The command is logged verbatim in the run manifest. No remote publication.

Limits: `MemoryMax=18G` (18 GiB), `MemorySwapMax=0`, process RSS watchdog 17 GiB, system `MemAvailable` floor 6 GiB, GPU total-use guard 7000 MiB, timeout 900 s. Stop on any swap/OOM/cgroup max event, I/O failure, corrupt/nonfinite output or other resource guard. Inspect initialization, prefill and decode separately, plus page-cache charge and direct file descriptor. Inspect whether both requests finish with EOS or at the 256-token cap. Public answer checks are 391 for `17*23` and JSON fields city=Porto, day=26, amount=14.20. These are functional checks, not proofs of numerical parity. Decode tok/s per request and process read bytes are exploratory; no 2 tok/s sustained claim from two requests alone. M4 passes only with both requests, resource integrity and source/numerical coverage stated honestly. Any failure keeps its own run identity; debugging uses a new one.

Alternative: stock 120B plain reference under the host budget was stopped by RSS guard during eager mmap prefetch; no comparable stock throughput is inferred. The bounded no-prefetch reference was used only for four-token parity. No optimization is introduced by this gate.
