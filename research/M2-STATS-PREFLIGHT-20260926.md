# M2 cache-counter diagnostic gate

Objective: measure target expert cache hits, demand misses, cold misses and blocking wait before choosing any throughput intervention. Base lab commit `a2fbb63dc4b1c0f5a08c67a74cdb25504cc62c9c`; exact original baseline `target120b-gpu8-two-turns01` is retained. The current candidate policy is 16 slots per layer, no speculative preload, four O_DIRECT workers, GPU8, unchanged top-4 router and MXFP4 weights. The second request's 1.9981106 decode tok/s is below the strict 2 target; the first was 2.2301784. This single pair is exploratory, not sustained M5 evidence.

Prospective diagnostic-only source change in the isolated `streaming-reference` worktree: on model destruction, after I/O workers join, print the already existing `llama_moe_stream::print_stats()` counters only when `TESY_MOE_STATS_AT_EXIT=1`. The prior mmap no-prefetch patch is inactive because streaming sets `use_mmap=false`, and `TESY_MMAP_NO_PREFETCH` will not be set. No cache selection, read, upload, tensor, router, kernel or sampler logic changes. The patch will be committed locally and recorded in `patches/`; the original baseline worktree/build remain untouched. This is an accounting diagnostic and consumes no optimization intervention. Metric: reported hit/miss/cold/wait plus process-attributed read bytes and cgroup/GPU peaks for the same two-request workload. Stop on differing token output, nonfinite values, swap/OOM/cgroup max, I/O error or guard. Any performance comparison with original is exploratory at one run per build and is not used to claim an M3 gain.

Run the same `workloads/m1_two_turns.txt` under 18 GiB host, swap zero, 7000 MiB GPU guard, 900 s timeout and the same CLI flags as M4. The existing stats combine ready hits with possible in-flight hits; preload is disabled, so ready hits dominate in this run. Source stats are cumulative across load/prefill/decode and both requests; `/proc/PID/io` 1 s samples and server timing logs permit approximate phase attribution but not exact physical device traffic. Encoded expert bytes per miss are 13,219,200 for this GGUF; O_DIRECT alignment and extra reads must be separately acknowledged. H2D bytes, per-layer counts, exclusive physical NVMe traffic and overlapping critical-path fractions remain unknown unless additionally instrumented. This diagnostic can support a prospective slot-capacity A/B only if miss/byte/wait observations justify it.

The diagnostic command uses `-lv 4` instead of the original run's `-lv 3`, because the existing `print_stats()` emits INFO logs. This changes logging volume only, not model computation. Compare outputs exactly, and treat timing as exploratory because log volume differs. The run script is `tools/run_target_stats.sh`.

First instrumentation check `stats20b-gpu8-smoke01` completed and matched the original eight-token text, but emitted no cache counters. The destructor hook is not observable through this CLI shutdown sequence; its attempt is retained in `patches/stats-at-exit.patch` and the failed diagnostic run manifest. Prospective repair 1/2: remove that hook and call the public `llama_moe_stream_print_stats()` explicitly in `tools/cli/cli.cpp` after the inference thread joins, before model teardown, guarded by the same environment flag. No model computation changes. Verify on a new 20B smoke identity before any target run. If this also fails, return to the last working diagnostic build rather than repeatedly editing the same failed hook.
