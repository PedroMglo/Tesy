# First real bring-up handoff

Date: 2026-09-23 UTC. Branch: `research/first-real-bringup-20260923`. Base: `research/novelty-runtime-foundation-20260923` at `a1aada5f24fb06d66b08566bf0d127ec3b78f994`. Campaign HEAD: `08296f10c74c7edce6c12a265e52c6e504427c9c`. Published result HEAD before this handoff commit: `cda30fc6411e26881b1931c56f8563b8061ee861`. The handoff commit itself becomes the branch HEAD after push. Draft PR: https://github.com/PedroMglo/Tesy/pull/4.

Observed host: physical Linux laptop, AMD Ryzen AI 9 HX 370 (24 logical CPUs), NVIDIA RTX 4060 Laptop GPU (8,585,740,288 bytes), driver 615.71.09, 32,698,654,720 bytes RAM, 16,348,868,608 bytes zram swap, internal `/dev/nvme0n1p3` model mount. Reference doctor PASS, no competing GPU compute process at snapshot. Toolchain: CUDA 13.3.73, GCC/G++ 15.3.1, CMake 4.3.0, compute 89, Release, four build jobs. `nvcc` was installed outside PATH; GCC 16 was unsupported by CUDA 13.3, so the installed GCC 15 was selected. No system installation or driver change was made.

Model: locked `gpt-oss-20b-mxfp4.gguf`, 12,109,564,352 bytes, SHA-256 `52f57ab7d3df3ba9173827c1c6832e73375553a846f3e32b49f1ae2daad688d4`; backend llama.cpp `4e416ee7308dd6b581796f1a6241276cd5982691`.

Campaigns: `results/bringup-20260923T203354Z` **FAIL_NATIVE_BUILD** with a missing CUDA object at link after accidental overlap of native builds; the shared-build race is the likely cause, though the file-removal operation was not captured. `results/bringup-20260923T204412Z` was **ABORTED_CONCURRENT** before model execution; `results/bringup-20260923T205424Z` was **PASS_DIAGNOSTIC_REFERENCE_BRINGUP_PIPELINE** on a fresh native build tree. All three output roots remain preserved. An exclusive runner lock and cheap contention test prevent recurrence. Intermittent auxiliary `nvidia-smi` failures during the first build remain unexplained; the successful campaign's doctor/device probe passed.

PASS: pinned stock and tracer CUDA builds, stock 16-token smoke, 16 generated token IDs equal OFF/ON, 15 one-token graph signatures consistent, count-space LRU/Belady derivation, GGUF expert inventory and byte-weighted trace simulation. Local `ruff check .` PASS; `pytest -q`: 54 PASS; shell syntax PASS. `--ngl 0` offloaded zero weight layers but reserved a 2.20 MiB CUDA compute buffer.

Quantitative: stock smoke wall 8.92 s, max RSS 12,353,100 KiB, zero swaps. Trace: 1,440 one-token expert accesses, 429 unique layer/expert keys. At 128 slots LRU made 838 loads vs offline Belady 549; at 256 slots 526 vs 429. Inventory: 24 × 32 experts, 13,253,760 encoded bytes per expert. Hypothetical 16 GiB RAM / 4 GiB VRAM LRU: 5,685,863,040 NVMe→RAM and 6,441,327,360 RAM→VRAM encoded payload bytes over 15 decode-step proxies. These are trace-derived simulated bytes, not measured I/O or bytes per verified committed token.

NOT_RUN: TTFT/TPOT benchmark, in-run VRAM peak, sustained thermal/resource telemetry, physical NVMe/PCIe expert traffic, stock B0/B1 comparison, chat validation, >RAM execution, native expert cache, speculation, vLLM B4, gpt-oss-120b. Full raw-artifact hashes, gate table, commands and limitations are in `research/results/first-real-bringup-20260923/`. No model weights, raw traces or large logs were committed.

Decision: retain `BROAD_NOVELTY_NO_GO / ENGINEERING_AND_MEASUREMENT_GO`, `STATIC_NO_GO_B2_GPTOSS_EXPERT_LAZY`, and vLLM as B4 comparator. Next discriminating gate: freeze a stock B0/B1 workload and collect actual placement, TTFT/TPOT, RAM/VRAM/swap, thermal and storage/PCIe telemetry before choosing a native cache or scheduling policy. The full Python dependency environment must be pinned prospectively for confirmatory work.
