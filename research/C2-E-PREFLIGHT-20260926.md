# C2-E frozen synthetic task suite before new model responses

Objective: compare practical utility of the best C2 sustained-qualified 120B profile with the pinned stock 20B on novel authorized synthetic tasks. Base campaign commit `16725cf` before this freeze; no campaign 2 model response has been generated. `workloads/c2_tasks.json` SHA-256 `11c09d2671adbea0214477a2bbb38e2a84a65dbfae1a01b7ce98123f8a74ab02`. `tools/c2_task_grade.py` SHA-256 `1d1d75095ecca4d2507e0a9eb42fc7808feee32e504d037488fa6ca32f5f34c0` at freeze. All sixteen reference answers pass, and one deliberately wrong mutant for each fails in a host-side no-network bubblewrap self-test. Three code tasks have multiple assertions, SQL tasks have two fixtures each including NULL/duplicates/zero cases; JSON tasks use exact types/keys/values and lexicographic tie rules where relevant. This validates the test mechanism, not model quality. The old M6 set is untouched.

Four development tasks and twelve evaluation tasks are frozen. The prospective **primary eight-task core**, selected for the available two-hour end reserve before any model output, is: `c2-eval-code-01`, `c2-eval-code-02`, `c2-eval-sql-01`, `c2-eval-sql-02`, `c2-eval-quant-01`, `c2-eval-plan-01`, `c2-eval-spec-01`, `c2-eval-spec-02`. The other four eval IDs (`code-03`, `sql-03`, `quant-02`, `plan-02`) are a separately identified optional extension only if their **entire paired** run fits the remaining budget before responses. No subset may be chosen after seeing either model's answers. The core has two code, two SQL, one quantitative, one planning and two specification/log tasks.

Generation policy for both: explicit medium effort, greedy temperature 0/seed42, one attempt, maximum 2048 completion tokens, context4096 with actual templated prompt length checked to leave the full output reserve. No prompt cache reuse; each model process separately, same semantic prompts. Timeouts must allow the slow target to consume the cap while remaining inside the campaign deadline. Historical target 24-slot 2.733 tok/s suggests 8*2048/2.733 = approximately 5995 s of decode if every core task reaches cap; prefill, startup and closure may make this exceed the 2-hour reserve. Therefore launch the core only when the remaining campaign time still accommodates its frozen worst-case envelope and closure; otherwise mark evaluation incomplete, never cut the run after seeing answers. A 32-slot rate from short A/B is **not** substituted for sustained rate in this estimate.

Measure each task's pass/fail/truncation/timeout, prompt and completion tokens including reasoning when available, first received text/final content, wall time, resources, and backend timing. Automatic validator environment error is a campaign error, not model FAIL. Record exclusive 120B wins, exclusive 20B wins, ties and total time, including failures. Differences in backend, tokenizer and placement limit parameter-count attribution. No significance claim from eight or twelve cases.

Tests at freeze: `python3 tools/c2_task_grade.py --self-test` PASS on host, 16/16 gold PASS and 16/16 mutants FAIL. Model responses NOT_RUN. Next gate: C2-B target numeric coverage, then sustained 32 and later task launch only if the strict C2-A gate and time/resources permit.

## Runner/validator adapter, prospective update before any C2 task response

On 2026-09-26 15:02 UTC, `tools/c2_task_grade.py` gained an adapter for the new `c2-server-raw-v1` run manifest and the already frozen eight core IDs. The original rubrics, inputs, gold and mutants are unchanged. The adapter requires all eight IDs in the frozen order, a complete successful server run, and labels any `finish_reason != stop` as `FAIL_TRUNCATED` even if the visible text happens to satisfy a validator. It records prompt and total completion tokens, reasoning/final character counts and request wall time. The nonstreamed API runner does **not** measure TTFT or first final content; those fields will be NOT_MEASURED for the core unless a separate streaming profile is run. No timing is inferred from response chunks. The 16 gold/mutant pairs still passed on the host after this adapter, and a synthetic eight-result manifest with one gold answer marked truncated scored exactly 7/8. This update precedes all core8 model answers and does not revise old M6 grading.

At 15:12 UTC, before any C2 core8 model answer, the exact stock20B GPU14, target120B GPU8/32-slot and fallback target120B GPU8/24-slot protocol identities were frozen in `workloads/c2e_core8_*_protocol.json`. Each uses the same semantic core8 prompts, medium/greedy/seed42, cap2048, context4096, no prompt cache, per-request timeout1200 s and total timeout7200 s. The target32 option is selected only if its fixed sustained32 timing/resource gate passes; otherwise the historical 24-slot profile may be used for an explicitly limited pilot or the paired evaluation deferred. No option is selected using task answers. The C2-B numerical block applies to either target placement; this suite cannot by itself turn it into robust exactness. The paired launch still requires a prospective budget estimate using completed sustained data; freezing these candidates is not a claim that all can be run.
