# M5 continuous server run: frozen secondary criterion met

Objective and prospective gate: `research/M6-PREFLIGHT-20260926.md` fixed a separate M5 criterion before the target task run. Its base lab commit was `6ccd72a`. The earlier eight-request CLI campaign remains `TIMEOUT` in `research/M5-CLI-RESULT-20260926.md`; it is not relabelled. Evidence class here is a measured single continuous server run, `task120b-stream24-eval01`, on the verified physical laptop. Raw stdout, stderr and one-second resource samples are retained locally outside Git. The JSON manifest and `results/task120b-stream24-eval01-analysis.json` contain the exact command, binary/library hashes, request timings, conditions and analysis.

Configuration: verified `gpt-oss-120b` MXFP4 GGUF, revision `238abdd290bb874b90a5da1b4549881b7d05c091`, SHA-256 `582bd40f6886200101f4c4ed9f25f3fe80cc14c86e9e2b37746cd8904a0c622d`; unmodified streaming backend `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`. GPU8, 24 expert slots per layer, O_DIRECT expert reads, no preload/repack, context4096, one sequence, batch256/microbatch32, eight CPU threads, greedy seed42, explicit medium reasoning, no prompt cache, max512 per task, one attempt. The server was bound to `127.0.0.1`, in an enforced 18 GiB host cgroup with zero swap. Watchdogs guarded RSS17 GiB, total GPU7000 MiB and system MemAvailable6 GiB.

All ten frozen task requests completed in one server process over **1342.188 s (22m22s)**, including server readiness and one separate simple streaming smoke. Backend timings covered all ten requests and token counts matched the API. The measured backend decode total was **2294 tokens / 839.31209 s = 2.73319 tok/s**. Last five requests decoded at **2.83084 tok/s**. Individual rates in task order: 2.512, 2.606, 2.708, 2.936, 2.541, 2.500, 2.877, 2.940, 2.839, 2.833 tok/s. All are unrounded above 2.000. Prefill total was 1094 tokens / 445.418 s = 2.456 tok/s. The sum of API request wall times was 1284.820 s; backend decode excludes prefill and server overhead, while the whole-process elapsed includes them. The server stayed in continuous use for 22 minutes; decode time itself was 14 minutes distributed among requests. This is not a claim of 22 minutes of uninterrupted decode or of 2 tok/s end-to-end task completion.

For a separate short streamed smoke, first received text/reasoning chunk arrived at 35.113 s and first final-content chunk at 50.043 s. These are client chunk arrivals, not per-token latencies; p50/p95 token intervals were not available. Ten task requests had completion text and backend timing; their quality is assessed separately in `research/M6-RESULT-20260926.md`. No prefix cache was reused between independent requests. The CLI run's cumulative-conversation prefill and timeout are a different named configuration.

Peak sampled cgroup memory was 11,711,664,128 bytes (10.91 GiB), process RSS 11,946,754,048 bytes, and total GPU used 3112 MiB. Swap, cgroup max/OOM events and watchdog stops were zero. The last sampled process `read_bytes` was 2,044,343,881,728 bytes across load, smoke, prefill and decode, **not** exclusive physical NVMe traffic. The model FD was observed O_DIRECT while decoding. Five-minute window median CPU Tctl was 64.25, 66.25, 65.0, 65.125 and 65.125 C; maxima by window were 86.5, 88.125, 87.125, 87.5 and 86.75 C. GPU temperature peaked at 58 C and NVMe composite at 61.85 C. No late-window decode-rate collapse is observed in these ten requests; power limits and clock throttling were not measured.

Decision: **M5_PERFORMANCE_TARGET_MET for this identified continuous localhost-server configuration** under the prospectively frozen secondary criterion: all ten complete, elapsed >900 s, complete backend timing, aggregate and last-half decode >=2.000 tok/s, zero swap/OOM/guard. The CLI M5 campaign remains a timeout, and 4 or 8 tok/s were not achieved. Correctness remains limited to the fixed-prefix and functional coverage described in M4/M6; long-prefix numeric equality and separate per-tier physical bytes/hits remain unknown. No optimization change was made for this run. Next discriminating gate was the paired task judgment, completed in M6.
