# C2-C sustained 32-slot preflight (frozen before server run)

Evidence class: PROTOCOL_FROZEN, NOT_RUN. Objective: test sustained 32-slot target performance only after C2-A and expanded C2-B evidence. No performance result is inferred from this document.

Base lab branch: `campaign/c2-correctness-usability-20260926-1349utc` from `c67529e`. Target artifact SHA-256 `582bd40f6886200101f4c4ed9f25f3fe80cc14c86e9e2b37746cd8904a0c622d`; streamed backend SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`. `tools/c2_server_run.py` uses existing server, resource helpers and strict `tools/c2_gate.py`; no model/backend source change. Exact binary/library/workload/configuration hashes are in `workloads/c2c_sustained32_protocol.json` and the separate 24-slot control protocol.

Both protocols use the ten original evaluation tasks in their original order, repeated as fixed blocks 1 and 2 in one process. The twenty run IDs include block number. Same `-ngl 8 -c 4096 -np 1 -b 256 -ub 32 -t 8 -tb 8`, `--no-warmup --no-cache-prompt --no-repack --no-op-offload --direct-io`, four expert I/O threads, `--moe-stream-direct`, no preload, medium effort, greedy temperature zero, seed42, cap512. Only per-layer expert slots differ: 32 versus 24. The first query is part of measured service; no separate warmup query. Task results include reasoning tokens in total completion counts.

Each run has a total timeout 3600 s and each request timeout 360 s. Fixed gate: all twenty requests and one-to-one backend timers, at least 900 s of actual request activity and 600 s of decode, aggregate decode at least 2.000 tok/s and chronological second half at least 2.000 tok/s. The 4 tok/s target is reported separately, not used to reclassify the 2 tok/s sustained gate. If requests end before the duration floor: NOT_SUSTAINED. If timeout cuts the fixed sequence: INCOMPLETE. No ad hoc extra requests.

Envelope: new delegated cgroup with `memory.max=19327352832`, `memory.swap.max=0`; RSS <=17 GiB, total GPU <=7000 MiB, host MemAvailable >=6 GiB, CPU Tctl <=95 C, GPU <=80 C, NVMe <=70 C. No max/OOM event increment. Samples about once per second, max gap3 s, boundary2 s, stable PID, memory.peak and local/hierarchical events. Stops and missing data fail. Normalization joins each response to one unique backend task using prompt/decode counts and times, with ordering checked; it never truncates a zip. `tools/c2_gate.py` checks exact raw request and sample schema. Instrumentation overhead is to be smoke checked with a short model request before sustained timing. 24/32 causal speedup requires both new runs; historical short A/B is a separate comparison.

Run only after the C2-B decision. Raw outputs, gate reports, and failures get unique IDs. No remote publication, source/model changes, resource-limit changes or replacement of historical reports are authorized by this protocol.

## C2-B dependency decision, frozen before sustained32 launch

At 2026-09-26 14:59 UTC, both permitted independent full 120B references had failed their secondary RSS guards, so C2-B concluded NUMERIC_COVERAGE_BLOCKED/PARTIAL. The single-request target32 runner smoke `c2c-target32-server-smoke03` passed the strict schema/resource/telemetry gate, including the unchanged 2 s boundary; its 14.069 s decode is not sustained. The source fix kept the sampler alive through server shutdown. `c2c-target32-server-smoke02` remains a gate FAIL for incomplete tail coverage. The next fixed 20-request 32-slot run is authorized only as **performance and resource diagnostic**. Even if its standalone C2-C duration/throughput gate passes, this will not qualify production-placement numerical correctness, promote a robust daily-use profile, or justify an optimization intervention while C2-B remains blocked. The frozen numeric thresholds and 20-task order are unchanged. A fresh 24-slot control is needed for any causal sustained speedup claim.
