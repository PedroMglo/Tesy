# Checkpoint N1 — falha histórica acessível; diagnóstico congelado

**MEDIDO na campanha histórica, recomputado localmente agora.** O caso original permanece FAIL: layer 2, route `[4,0,31,17]`, GPU resident `{0,1,2,3}`, `h=1`. O FFN mixed tinha `relative max=4.12584316e-05`; os logits completos tinham `relative max=0.008559786656194158`, acima do limite `0.005`, embora o greedy argmax coincidisse. Isto **não** atribui causalmente a falha ao operador GPU: peso host correcto e all-CPU shadow bitwise não substituem device readback, per-expert tracing nem intervenção FFN→suffix.

Os bytes originais dos dois vectores F32, `failure.json`, argv, build/runtime provenance e telemetria estão em [`vertical-numerical-blocker-20260925/`](vertical-numerical-blocker-20260925/). O [manifesto](vertical-numerical-blocker-20260925/evidence-manifest.json) fixa SHA-256 e tamanho de cada cópia; o [diagnóstico recomputado](vertical-numerical-blocker-20260925/historical-logits-diagnostics.json) dá max abs, referência, relative max, cosine, argmax, top-2 e maiores erros. A reprodução usa `numpy.fromfile(..., dtype='<f4')`, diferença em float64 e cosine em float64. O log de primeira divergência é um excerto literal da root original; o stderr completo continua na root local e no bundle público C3.

Telemetria original: 38 linhas, das quais 36 amostras têm simultaneamente GPU OK e dados de processo; duas linhas terminais não têm processo e não provam swap. Nas 36 válidas, swap observado 0, peak RSS 13 125 591 040 bytes, peak GPU used 918 552 576 bytes, minimum MemAvailable 23 582 863 360 bytes, temperatura GPU 46–51 °C. Isto é health observada até à falha, não aprovação de uma campanha FAIL.

O [protocolo diagnóstico](VERTICAL-NUMERICAL-BLOCKER-DIAGNOSTIC-PROTOCOL-20260925.md) congela a ordem dos controlos antes de novos runs. Device readback, contribuições por expert, associação das somas, intervenção FFN→suffix, primeira divergência downstream e correctness recuperada: **NOT_RUN**. Branch base `ca532aaa765ad551dcb8be4424f0abe6778eec96`, tree `b96da0aa9441a562c1511d9e27f8db191ac0d91a`; measurement commit da falha `6f9beefc6b346b2a30d5e4a4af05c28f934d9289`. Auditoria externa pendente.
