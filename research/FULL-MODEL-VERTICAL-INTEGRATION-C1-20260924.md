# Checkpoint C1 — agregação corrigida e geração stock

Estado: **MEDIDO parcialmente**, C1 de engenharia concluído; C2/C3 `NOT_RUN`. Base `4e6501fc3afaab22bf152fc8e5a9ebb1cd336222`, tree `5084b5ef8e6587895b9390fa79adbad6ab0cf307`. Commit de implementação/medição C1 `d9b57c31a3ad178e950469284c3f8d38033cc1a8`, tree `fd982484ce27bd659dec6024c8dfe13e4d32611c`.

O `make_sum_graph` agora usa dois leaves próprios; há cópia explícita do FFN GPU já calculado antes da soma. `tesy-sum-graph-model-free` verificou topology e execução, incluindo contador FFN igual a 1 após a soma. O component diagnostic de agregação também inclui essa cópia. Timings antigos de mixed/handoff **não** são resultados do executor corrigido e não autorizam comparação com ele. O graph corrigido foi compilado com `-Werror` no build patched existente. Ruff, compileall, shell syntax e pytest completos: **302 passed**; `git diff --check` PASS.

Comando utilizável: `bash scripts/run_full_model_stock.sh MODELO benchmarks/prompts/vertical-short-summary.txt ROOT 0 128`. Usa o `llama-cli` stock sem patch, template `gpt-oss`, greedy, contexto 4096, 12 threads, `n_gpu_layers=0`; o runner recusa identidade de modelo/host inválida e roots existentes. A geração de 128 tokens máximos completou em `results/full-model-stock-generation-20260924T231800Z/`, sem `failure.json`. Modelo/prompt/binário/argv e host estão registados nessa root. Host de referência físico PASS; 60 amostras de processo válidas, zero GPU failures e peak process swap 0. Peak process RSS 12 764 119 040 bytes, uso GPU observado 333 447 168 bytes, temperatura observada até 46 °C. Uma amostra terminal sem campos de processo foi excluída da afirmação de swap.

**Limitação de formato/qualidade:** apesar de produzir texto, a resposta stock ao pedido de resumo começou por texto não relacionado sobre `gpt-oss-2` e código Go. Não classificamos este output como resumo útil. Pode ser comportamento do template/configuração ou do modelo; fica `DESCONHECIDO` até diagnóstico separado. Os 128 tokens foram um limite de geração, não uma contagem independente extraída do tokenizer. A comparação final e B_competitive continuam `NOT_RUN`. Não há claim de speedup.

Root falhada preservada: `results/full-model-stock-smoke-20260924T231500Z/` falhou admissão de host dentro do sandbox (`container-other`, GPU invisível) antes da geração. A root sucessora física `results/full-model-stock-smoke-20260924T231600Z/` completou 16 tokens máximos; é apenas smoke. Os ficheiros completos destas roots estão **apenas locais**, não auditáveis remotamente por hash; os seus hashes/paths constam do manifesto C1. Assinatura SSH de commit indisponível sem chave física; o commit C1 foi feito sem assinatura, sem alterar configuração global.

Próximo passo interno: resolver a configuração de formato/qualidade stock, construir o caminho live de routing variável e controlo negativo, e só depois congelar/medir C3. Nenhum resultado físico de Tesy corrigido foi produzido neste checkpoint.
