# Reparação técnica da captura diagnóstica (não do candidato)

Root preservada: `results/vertical-numerical-diagnostic-20260925T232053Z/`, measurement commit `8218252f9a5e8d46096bb11995e7a5b92f566987`, tree `a7e723edd00dd97315c1ed089aaedfd590ccd157`. O run reproduziu a falha original (`FFN relative max=4.12584316e-05`, full-logit `0.00855978666`); confirmou readback bytewise dos seis tensors GPU e de activation/IDs/weights nos inputs compactos. A mapping observada foi slot 0→expert 4 CPU local 0, slot 1→expert 0 GPU local 0, slot 2→expert 31 CPU local 1, slot 3→expert 17 CPU local 2.

**Defeito da instrumentação:** os estágios do graph compacto foram lidos depois do compute completo. O backend podia reutilizar buffers intermédios; por isso os sidecars de estágios desta root não qualificam a localização da primeira operação. A análise offline assinalou precisamente essa suspeita: vários estágios GPU pareciam diferir em `relative max=1`, embora `down_biased` já diferisse apenas `7.37e-05`. Não foi atribuída causa física a esses snapshots.

**Correção:** recomputação diagnóstica separada em views contíguas do mesmo graph compacto, capturando cada estágio imediatamente depois da view que o materializa. O FFN final desta replay tem obrigatoriamente de coincidir bitwise com o output do compute normal anterior; se não, o diagnóstico falha. A replay não alimenta o decode committed e não entra em timing. O teste nativo model-free agora verifica captura imediata de dois estágios e igualdade bitwise do output após replay. O protocolo N1, route, residência, workload e thresholds permanecem intocados. A próxima tentativa usa nova campaign identity.
