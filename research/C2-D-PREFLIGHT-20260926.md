# C2-D latency diagnostic preflight

State at 2026-09-26 15:13 UTC: PROVISIONAL INPUT, NOT_RUN. C2-B target numerical coverage remains blocked, so latency measurements cannot promote a new runtime configuration. No backend optimization is authorized until correctness is restored; optimization interventions used in C2 so far: 0/6.

The draft `workloads/c2_latency_prompts.json` contains three deterministic synthetic incident-log prompts with an objectively checkable unique ticket answer. Target post-template lengths are approximately 128, 512 and 1536 tokens, with 512 output tokens reserved under context4096. The current file SHA-256 is `5e7986d95001358a99097b29dfc4a71a98184e105d93a3beafd35b5d287a0299`. These are draft lengths, **not** measured token counts or a frozen measurement workload. After C2-C stops, call the same pinned server's `/apply-template` and `/tokenize` endpoints without inference; adjust record counts if needed, then freeze final file/hash and thresholds before any latency response. Do not touch the running C2-C server.

`tools/c2_latency_client.py` measures client request to first nonempty reasoning/final text chunk, complete response, API usage when streamed, and exact templated prompt token count. SSE chunks are not per-token intervals; p50/p95 token times remain NOT_MEASURED. For the short prompt (<=128 post-template tokens), the prospective goals remain first text <=10 s and first final content <=20 s, with medium effort, greedy/seed42, context4096 and max output512. Output text, reasoning text and finish reason are retained locally; correctness of the unique ticket is checked separately. Process resource samples and backend timings come from a bounded localhost server with the same 18 GiB/zero-swap production guards. The client code's SSE parser passed a model-free synthetic event test. `tools/c2_stop_server.py` checks the sampled PID, process group, model argument, loopback bind and cgroup before signalling only that process; unrelated-PID rejection passed model-free.

The client now derives the one CRITICAL/OPEN ticket from each frozen synthetic prompt and requires exactly `{"ticket":"T..."}` for a verifiable final answer. This check was added before any latency generation; the draft prompts contain exactly one target each. Time to verifiable completion is reported only on that exact match.

Cold expert-cache process, warm model with a new prefix, and exact prefix reuse are distinct profiles. The existing comparable server command has `--no-cache-prompt`; exact-prefix reuse is NOT_RUN unless a separately frozen supported profile is created. No global cache clearing is used. A count-only process may warm host file cache but does not decode; that condition will be recorded, not called a cold NVMe run. C2-D chooses an intervention only if prior phase accounting and C2-B allow promotion; otherwise it reports baseline latency and bottleneck uncertainty.

## Frozen measurement input after official count-only probes

At 2026-09-26 15:39 UTC, before any C2-D generation, the pinned target server's `/apply-template` and `/tokenize` endpoints measured the final three prompt lengths as **113, 496, 1522 tokens** in short/medium/long order. Draft count probes were 164/596/1676 and 145/496/1522; the short prompt was shortened until it met the previously declared <=128-token goal. These count-only probes generated no answers. The final input is `workloads/c2_latency_prompts.json` SHA-256 `0190f1cef2228a0937005a4c8a2bf790b3e7e49bce9782c52d0f857a8baf20c0`; the authoritative count file is `results/c2d-target32-latency-count03.json`. All prompts reserve 512 output tokens in context4096. Targets and success rule are fixed before responses. The server launched after a full read-only O_DIRECT model hash preflight and reused the 32-slot profile, but count-only operations and prior C2 runs may leave host storage caches warm. The first generation in this process is cold only with respect to its expert slots. Short goals remain first text <=10 s and first final content <=20 s; medium/long results are descriptive. No response has been observed at this freeze.
