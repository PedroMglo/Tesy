# Raw llama-fit-params stdout is preserved byte-for-byte as evidence.
# Upstream emits a trailing space before newline; do not normalize it.
auto-fit.stdout.txt -whitespace
capacity/*.stdout.txt -whitespace
