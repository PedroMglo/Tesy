# M5 sustained 120B preflight

Prospective sustained run, conditional on the third 16/24-slot A/B pair preserving outputs, exact prefix equality and limits. Base lab commit `aa97129`. Objective: test the fixed >=2 decode tokens/s target after continued use and report full windows, resource and thermal trends. The candidate remains original streaming SHA `1248fd8fa8cfebaece5ea992e4d951c1e18bb9d5`, verified 120B GGUF SHA `582bd40f6886200101f4c4ed9f25f3fe80cc14c86e9e2b37746cd8904a0c622d`, 24 slots/layer, GPU8, direct expert I/O, no preload/repack, one sequence/context4096, batch256/microbatch32, greedy, max256 tokens per response. Eighteen GiB host cgroup, zero swap, watchdog RSS17 GiB, free RAM floor6 GiB, GPU cap7000 MiB, max 1800 seconds. `workloads/m5_sustained.txt` has eight fixed public prompts and an exit command; one process keeps context across requests. These prompts probe sustained work, not the held-out task benefit gate.

Primary measure: total decoded tokens divided by sum of all backend decode times, using unrounded values; report each of eight request rates, first and last four aggregate rates, and full end-to-end elapsed with prefill included. The >=2 target is met only if whole-run decode and last-half decode both remain >=2.000 tokens/s, the run lasts at least 900 seconds, all requests complete or naturally EOS, and no swap, OOM, resource guard or backend error occurs. If below 900 seconds, repeat a new prospective workload/run identity rather than relabel this one as sustained. Report prefill and final text token counts separately. Sampling gives cgroup/RSS/GPU peaks, process read bytes, GPU temperature/power windows; CPU thermals are separately measured if sensors expose them. TTFT will be measured in a later server streaming request; CLI timing alone does not isolate it. A TTFT gap remains an explicit limitation until that request succeeds. No load/prefill time is removed from end-to-end latency.

Exactness evidence entering the run is the 4-token complete-logit equality versus plain reference and greedy output equality on two requests in 24-slot A/B; those do not prove numerical equality for every new prefix here. Any visible corruption, missing expert, resource violation or backend error stops promotion and this run. Output root `results/target120b-gpu8-sustained24-01*`, local-only publication. Tests: NOT_RUN at freeze. No threshold, sampler, context, template or expert policy will be changed after observing the result.
